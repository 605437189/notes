#### 栈stack
1.用两个栈实现队列
2.包含min函数的栈


#### 设计类
1.设计LRU缓存结构
	 思路：
	  1.利用MAP结构的存取复杂度皆为O(1)  Plus:map的key本身也是按新增顺序存储的
	  2.使用双向链表 头节点为最近存取数据
2设计LFU缓存结构
	  思路1：
	  1.利用MAP结构的存取复杂度皆为O(1) 
	  2.使用双向链表 排序  每次更新node遍历链表插入位置 时间复杂度O(n)

#### 回溯类